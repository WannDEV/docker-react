language: generic
sudo: required

services:
    - docker

before_install:
    - docker build -t matthiasjensen/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true matthiasjensen/docker-react npm run test